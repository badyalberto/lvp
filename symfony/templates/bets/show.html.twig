{% extends 'base.html.twig' %}

{% block title %}Bets{% endblock %}

{% block body %}
    <h1>Bets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bet.id }}</td>
            </tr>
            <tr>
                <th>Kills</th>
                <td>{{ bet.kills }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bets_index') }}">back to list</a>

    <a href="{{ path('app_bets_edit', {'id': bet.id}) }}">edit</a>

    {{ include('bets/_delete_form.html.twig') }}
{% endblock %}
